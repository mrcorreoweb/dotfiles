# This tells Git to use the .gitignore_global file for global ignores.
[core]
    excludesfile = ~/.gitignore_global

# The Git push command will automatically create  
# a remote branch with the same name.
[push]
    default = current
